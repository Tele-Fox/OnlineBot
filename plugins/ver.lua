do

function run(msg, matches)
  return [[انلاین بات ورژن 1
ساخته شده توسط فاکس تیم

با این ربات میتوانید در سرور خود با ربات بازی کنید
]]
end
return {
  description = "Shows bot ver", 
  usage = "/help: Shows bot ver",
  patterns = {
    "^([/!]ver)$"
  }, 
  run = run 
}
