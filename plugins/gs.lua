do

function run(msg, matches)
  return [[بازی ها :
CounterSource (درحال ساخت)
Counter1.6 (در دسترس)
Call of duty4 (در دسترس)]]
end
return {
  description = "Shows bot gs", 
  usage = "/help: Shows bot gs",
  patterns = {
    "^([/!]gs)$"
  }, 
  run = run 
}
